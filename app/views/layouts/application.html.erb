<!DOCTYPE html>
<html>
<head>
  <title>TestProject</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %> <!-- This line adds the CSRF token -->
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
  
  <!-- Additional CSS styles -->

</head>
<body>

<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">Test Project</a>
    </div>
    <ul class="nav navbar-nav">
      <li class="<%= 'active' if current_page?(campaigns_path) %>"><%= link_to "Campaigns", campaigns_path %></li>
    </ul>
   <ul class="nav navbar-nav">
        <li><%= link_to "Users", users_path %></li> <!-- New link added -->
</ul>


    <ul class="nav navbar-nav navbar-right">
      <% if current_user %>
        <!-- User Icon with email -->
        <li>
          <a href="#">
            <span class="glyphicon glyphicon-user"></span> <%= current_user.email %>
          </a>
        </li>
        <!-- Logout Icon -->
        <li>
          <%= link_to destroy_user_session_path, method: :delete do %>
            <span class="glyphicon glyphicon-log-out"></span> Logout
          <% end %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>

<div class="container">
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  <%= yield %>
</div>

</body>
</html>
